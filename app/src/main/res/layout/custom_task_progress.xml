<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layout_custom_progress"
    android:layout_width="160dp"
    android:layout_height="25dp"
    android:background="@drawable/bg_progress_outlined_32"
    android:paddingVertical="1dp"
    android:paddingHorizontal="1.6dp">

    <TextView
        android:id="@+id/tv_task_progress"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:elevation="10dp"
        android:textAppearance="@style/LabelSmall"
        android:textColor="@color/black"
        tools:text="10/30" />

    <View
        android:id="@+id/task_percentage"
        android:layout_width="0dp"
        tools:layout_width="20dp"
        android:layout_height="23dp"
        android:background="@drawable/bg_progress_tertiarycon_32" />
</RelativeLayout>